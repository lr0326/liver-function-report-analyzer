version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: liver-analyzer-backend
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    volumes:
      - uploads:/tmp/uploads
    restart: unless-stopped

volumes:
  uploads:
